# ─────────────────────────────────────────────
# DECLUTTR — backend/requirements.txt
# Merged & deduplicated (conflicts resolved: newer version wins)
# Last updated: 2025-06
# ─────────────────────────────────────────────

# ── Web framework ────────────────────────────
fastapi==0.115.5                   
uvicorn[standard]==0.32.1         # includes websockets + httptools
python-multipart==0.0.6           # for form/file uploads in FastAPI

# ── Data validation & settings ───────────────
pydantic==2.10.3                  
pydantic-settings==2.1.0          # .env → settings class

# ── Database (local SQLite) ──────────────────
sqlmodel==0.0.21                  # wraps SQLAlchemy + Pydantic
sqlalchemy==2.0.23                # explicit pin for aiosqlite compat
aiosqlite==0.19.0                 # async SQLite driver
aiofiles==23.2.1                  # async file I/O

# ── Google APIs & OAuth ──────────────────────
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.107.0 

# ── Image processing & dedup ─────────────────
Pillow==11.0.0                    
imagehash==4.3.1                  # pHash for near-duplicate detection
scipy==1.14.1                     # imagehash dependency

# ── HTTP client ──────────────────────────────
httpx==0.28.1                    

# ── AI / XAI providers ───────────────────────
anthropic==0.40.0                 # Claude (primary XAI)
openai>=1.54.0                    # GPT fallback
google-genai>=1.0.0               # Gemini fallback
mistralai>=1.2.0                  # Mistral fallback
cohere>=5.11.0                    # Cohere fallback
groq>=0.13.0                      # Groq/Llama fallback

# ── Utilities ────────────────────────────────
send2trash==1.8.3                 # safe trash (non-permanent delete)
python-dotenv>=1.0.0              # .env loading

# ── Async runtime ────────────────────────────
anyio==4.7.0

# ── Testing ──────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
